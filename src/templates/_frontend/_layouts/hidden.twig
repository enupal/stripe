{%- set handle = craft.enupalStripe.labelToHandle(block) %}

<input type="hidden"
       name="metadata[{{ handle }}]"
       id="{{ handle }}"
       class="{{ handle }}"
       value="{{ preValue }}"
/>