{% spaceless %}
    <form action="{{ button.getReturnUrl }}" class="enupal-stripe-form" id="enupal-stripe-{{ button.sku }}" method="POST">
        <button id="customButton" class="enupal-stripe-button"><span>Purchase</span></button>
        <input type="hidden" name='enupalStripeData' value="{{ settings.getPublicData() }}">
    </form>
{% endspaceless %}